@page
@model WebApp.Pages_RecipeProducts.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RecipeProduct</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                
                <label asp-for="RecipeProduct.ProductAmount" class="control-label"></label>
                <input asp-for="RecipeProduct.ProductAmount" class="form-control" />
                <span asp-validation-for="RecipeProduct.ProductAmount" class="text-danger"></span>
            </div>
            <div class="form-group">
                
                <label asp-for="RecipeProduct.ProductId" class="control-label"></label>
                <select asp-for="RecipeProduct.ProductId" class="form-control" asp-items="ViewBag.ProductId"></select>
                <span asp-validation-for="RecipeProduct.ProductId" class="text-danger"></span>
            </div>
            <div class="form-group">
                
                <label asp-for="RecipeProduct.RecipeId" class="control-label"></label>
                <select asp-for="RecipeProduct.RecipeId" class="form-control" asp-items="ViewBag.RecipeId"></select>
                <span asp-validation-for="RecipeProduct.RecipeId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="RecipeProduct.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
